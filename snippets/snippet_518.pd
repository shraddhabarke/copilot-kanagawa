inline void set_max_stall_count(count_t<(MaxStallCount == 0 ? 1 : MaxStallCount)> maxStallCount)
    {
        sim_assert(maxStallCount <= MaxStallCount);
        _maxStallCount = maxStallCount;
    }